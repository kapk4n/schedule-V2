<%= form_with model: @user, url: ed_sub_path(id: params[:id]) do |form| %>

  <div class="mb-3">
    <%= form.select :subj, @list_of_subj.each {|i| i}, include_blank: true %>
    <%# <%= form.text_field :role, class: "form-control", placeholder: "role" %>
  </div>

  <div class="mb-3">
      <%= form.submit "Save", class: "btn btn-success" %>
      <%= link_to "back", administration_path , class: "btn btn-warning" %>

  </div>
<% end %>